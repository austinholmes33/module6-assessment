const {shuffleArray} = require('./utils')

describe('shuffleArray should', () => {
    test('shuffleArray should return an array', () => {
        expect(shuffleArray).toBe([])
    },

    test('', () => {
        expect(shuffleArray([i])).toHaveLength(shuffleArray.length)
    })
})